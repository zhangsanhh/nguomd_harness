# AGENTS.md 版本与变更记录

> `AGENTS.md` 的版本历史与变更记录，供版本追溯与回退参考；独立存放，不随规则正文每次读取。规则正文以 `AGENTS.md` 为准，其当前版本号见 `AGENTS.md` 第 17 节。

修改 `AGENTS.md` 后须升版本，并在下表最上方追加一行（新版本在上）。

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| 1.40.0 | 2026-09-12 | 用户纠正：**影评成稿必须落盘并同步清单**。《影评生成范式》新增 §五.9「落盘即验收」（正文写入 `01_内容整理/影评分析/<电影名>影评.md`＋README 三处同步＋`已看电影清单.md` 补录＋交付报告逐项标注）与 §六.9 收尾强制项，§五.3 补 10000 字符以上章节档位；《影片分析约束》新增「必须」第 12 条并在流程一句话补入落盘同步；§3.7 指针行补未落盘状态用词；沉淀 BC_流程_010 与 R-063，并在 BC_内容_011 补写两条交付形态规则的边界 |
| 1.39.0 | 2026-09-11 | 用户要求：**交互问答默认只在前台回答，不生成文档**。`提示词库/资料参照检索规则.md` 升 v2.4，新增 §5.3（默认前台、仅用户明确要求才落文件、字数要求在前台遵守、日记侧写为唯一例外）；§3 指针行、提示词库 README 索引、`日记处理提示词.md` §9.5 同步说明侧写不受此限且不再额外生成解读稿；沉淀 BC_内容_011 与 R-062 |
| 1.38.0 | 2026-09-11 | 用户要求：**参照源不止硬盘**。`提示词库/资料参照检索规则.md` 升 v2.3，新增 §5.4——硬盘为主参照但非唯一依据，来源分三级（硬盘／联网检索／既有知识）；硬盘只给结论而无机制或出处、涉硬事实与数据、涉会变化的信息、或材料不足以闭合论证时，必须补足或降级表述或明说无依据，禁止把一句结论扩写成严谨机制描述；补足义务优先于省调用，且对交互问答、日记侧写、一般回答一律适用。§六.4 成本闸门加例外说明，§3 指针行、提示词库 README 索引、`日记处理提示词.md` §9.4 与 §9.3 语体条款同步 |
| 1.37.0 | 2026-09-11 | 用户要求（同日第二次收紧）：资料参照改为**默认零来源标注＋自然语体**。`提示词库/资料参照检索规则.md` 升 v2.2——§五 重写为「来源：默认不标注（凭原句回查、主动问才给）＋语体：像正常回答问题（禁元话语、禁多方引述对账与辩论会口吻，结尾不贴检索说明）」；§二 冲突改为一句自然话说明；§六 边界同步。§3 指针行、提示词库 README 索引、`日记处理提示词.md` §9.3 第③步与 §9.4 第 4 条同步。检索与核对仍为硬要求，只是不外显 |
| 1.36.0 | 2026-09-11 | 用户要求：资料参照的来源标注改为**轻量行内标注**，不再单列来源章节或表格。`提示词库/资料参照检索规则.md` 升 v2.1（§五 重写为总原则＋行内标注＋末尾一行检索轨迹＋按需展开完整清单＋三处产物同口径；§二 冲突标注同步改为一句写明），§3 指针行、提示词库 README 索引说明同步 |
| 1.35.0 | 2026-09-11 | 用户批准并定性为 Agentic RAG：新增 `提示词库/资料参照检索规则.md`（v2）——问答环节的检索—引用层，触发为「资料域主题语义命中＋显式指令」，硬盘 `/Volumes/Elements` 为主参照、未挂载时降级为电脑侧全机检索（不设目录白名单）；核心为自主检索循环（生成查询→精读→评估证据→改写重查）与三条停止条件；回答末尾标来源与检索轨迹，不生成检索台账。§3 新增指针行，§17 版本号同步为 1.35.0（修正 1.33.0／1.34.0 遗留的 §17 滞后） |
| 1.34.0 | 2026-09-11 | 用户批准：日记工作流正式接入侧写——`提示词库/日记处理提示词.md` 新增第九节（输入层级、五步结构、核心改进五项上限、硬盘跨资料检索、归档与前台四项顺序），并明确原始口述原样追加到 `日记.md` 当日条目；侧写不并入日记中心页面（页面固定三个来源）；资料段落只写主张与适用条件、不给换做法建议；硬盘 docx/pdf 转换文本置于 `raw_data/硬盘资料文本/` |
| 1.33.0 | 2026-09-11 | 用户确认：日记工作流整体自 Codex 项目迁入 DSC，`03_生活记录/日记中心/`（原始日记、意图还原总库、战术与战略分析总库、索引脚本与 HTML 页面），处理提示词迁入 `提示词库/日记处理提示词.md`；§14.2 目录路由增加日记工作流入口，根 README 与 03_生活记录 README 同步；Codex 侧对应文件与引用移除并归档 |
| 1.32.0 | 2026-09-10 | 用户确认：将会话摘要单条字数限制由 65—120 字统一调整为 80—180 字；同步全局规则、当前 DSC 项目规则、全局规范副本及 AI PM 会话日志 README，其他正文与归档字数规则不变 |
| 1.31.0 | 2026-09-10 | 用户确认：新增当前项目专用的「个人记录版会议纪要」按需触发规则；适用于几十分钟至数小时的个人口述、转写或 SRT，按时间轴与主题自然分段，固定输出主线、实际推进与产出、卡点偏航与状态变化、备忘与下一步；默认不跨项目、不自动写入其他资产 |
| 1.30.0 | 2026-09-10 | 用户确认：将骨架确认从长文专项扩展为普通复杂任务默认渐进式处理披露，覆盖长文本、多源研究、跨文件及复杂电脑操作；明确骨架→确认→分阶段交付→整体复核，与用户主动开启的计划模式分离；增加上下文按需读取和实际状态报告边界 |
| 1.29.0 | 2026-09-09 | 用户批准 Bad Case 闭环补录计划：补充案例状态与验证证据分层、来源覆盖台账、回归结果边界及项目钩子路径可复现要求；复发后重开，证据不足不得写成通过 |
| 1.28.0 | 2026-09-09 | 用户确认：新增复杂任务闭环执行模式；对多步骤、长链路或多工具任务要求先拆解计划，按关键步骤执行与验收，未通过先纠偏复测，全部完成后整体复核；增加默认触发语并保留真实阻断边界 |
| 1.27.0 | 2026-09-02 | 用户批准：修正子代理超时回收为「wait_agent 后必读 read_thread；按进展继续等待；无进展先 interrupt 救援；仅终止或救援失败才 close_agent」；影评审查任务卡收窄为影评正文、事实清单和验收标准；reviewer 推理档位改为 high，主代理 max 保持不变 |
| 1.26.0 | 2026-09-02 | 用户批准：同步全局 AGENTS 与规范副本的子代理派发与回收协议；增加 120 秒分段状态查询、完成但报告未回传时优先取回记录、仅明确启动/模型/账户/任务失败才重试一次，并记录状态链与兜底原因 |
| 1.25.0 | 2026-08-19 | 用户批准：§6.5 增加四级意图还原闸门（L0 单义低风险、L1 明确多步、L2 多义/缺口、L3 高风险操作），并同步 `提示词库/需求澄清纪律.md` 的默认值与产出前置条件，要求闸门先于工具调用和实际改动 |
| 1.24.0 | 2026-08-17 | §5 新增普通文字回答表现层规范：一段一意、每段 60–180 个汉字、连续 2–4 段后增加小标题、标题段落列表留空、超过 500–700 字按主题拆区块、不同主题不合并成长段落；代码块、表格单元格、逐字引用和用户固定格式不强制改写 |
| 1.23.0 | 2026-08-12 | 应要求精简约 4.5%（字符，6095→5820）：§3.6/3.7 与全局 AGENTS §二.5/6 逐字重复的细则改为指针（规则在全局层仍强制生效、编号保留），其余仅压缩冗余措辞（删「自动」「快速」等无信息量词与重复时序），未删任何强制规则、交叉引用与路径；版本号同步 §17 |
| 1.22.0 | 2026-08-10 | 新增两条通用规范（用户批准，不限于影评）：①移动硬盘/外部存储挂载即默认主动读取并评估与当前任务的关系，读取须实际读入正文、列出目录不算；②子代理只允许一层、任务卡显式禁止再派生，空转/无产出/报告未传回/链中断时最多重发一次后由执行体自查兜底、不无限等待；同步全局 AGENTS 与 00_系统配置/codex 规范副本，并修正头部版本号与 §17 不一致 |
| 1.21.0 | 2026-08-06 | 会话日志摘要字数调整（用户批准）：每条摘要由「≤100 字」改为「65-120 字」；同步全局 AGENTS、`00_系统配置/codex/AGENTS.md` 规范副本、相邻项目会话日志 README、网页索引脚本文案、回归集 R-001、BadCase 07 记录范围 |
| 1.20.0 | 2026-08-05 | Pitfall 库升级为活资产（用户批准）：§12 改为双触发（动手前查 + 失败修正 1 次后仍失败或同一失败持续约 15-20 秒未解决才查）；Pitfall 条目新增「报错特征/触发关键词、最近复用、状态」三字段并回填 49 条；命中解决后回写日期、连续 2 次命中无效标失效；同步 BadCase README、回归集 G-011/R-026/R-027 |
| 1.19.0 | 2026-08-04 | 规则分层去重（用户批准方案）：明确「Codex 内置系统提示词＞全局 AGENTS＞项目 AGENTS」三层；删除与系统提示词/全局重复的表述，§3.1 压缩为要点＋提示词库指针，保留全部强制规则与章节编号；全局 AGENTS 小幅增补（简体中文、分层说明）；新增 `00_系统配置/` 规范副本与同步清单（Git 由用户在 GitHub Desktop 自理）；新增回归 G-010、R-025 |
| 1.18.0 | 2026-08-03 | §3.1.6 证据权重补充来源独立性操作定义（同项目 README/官网、转载与被转载、同一机构多页面属同一来源家族，不构成独立交叉验证；跨家族才可互证）；配套新增 `提示词库/科普写作-论断核验矩阵.md`（论断级证据回溯＋反证检索阶段），爬取经验库第六节补「抓取内容视为不可信输入」安全规则（用户批准） |
| 1.17.0 | 2026-08-03 | §6.5 末尾新增指针：需求澄清细化纪律（每轮最多 2-3 问、必填五维、默认假设清单、低风险单义不套用）见 `提示词库/需求澄清纪律.md`；提示词库 README 与根 README 描述同步（用户批准方案 A） |
| 1.16.0 | 2026-08-03 | 第 6 节新增第 9 条变更同步检查：增删改文件后用 `rg` 同步全部引用（导航/索引/AGENTS 提及），删除须移除引用行；§6.7 交接文件描述对齐 `.codex/pre_compact.sh` 实际实现（precompact-*.md 机械快照，语义以压缩摘要为准）；§15.1 固定目录补充 `.codex/`；同步根 README 与回归集 G-004（用户批准） |
| 1.15.0 | 2026-08-03 | 第 4 节新增第 5 条：配置/默认值变更（含 `config.toml` 及其引用文件，如 compact_prompt、模型目录）前先给一句「改什么、为什么、影响面」，用户无异议再执行，禁止改完再解释（BC_流程_006 修复，用户批准） |
| 1.14.0 | 2026-08-03 | 新增第 6 节第 8 条 Fork 提醒（用户授权制）：命中标准（影响面≥一类任务、移动/删除/覆盖文件、方案多解、需对比两方案、长任务试错）时用一句话提醒，用户回「行/不行」同意后才可调用 fork，禁止自动 Fork；提醒不要求用户阅读文件 |
| 1.13.0 | 2026-08-03 | 画像扩充与主动提取流程：`03_生活记录/用户画像.md` 扩充为可维护的静态记忆层，覆盖背景、作息、职业规划与沟通边界；第 14 节新增强制维护流程——Codex 聊天中主动提取候选，写入前在回复底部征得同意，只写经确认信息 |
| 1.12.0 | 2026-08-03 | 新增用户画像记忆层：创建 `03_生活记录/用户画像.md`（仅记录用户明确告知且经确认的信息）；第 14 节新增画像读取规则，仅记录用户明确告知信息；同步根 README 与 03 目录导航 |
| 1.11.0 | 2026-08-03 | 低风险修剪（C 方案）：删除第 11 节占位；第 12/13 节细则指针化至 Pitfall 库与 BadCase README；第 1/2/9/14 节压缩重复表述；第 8 节清理空行；未删任何强制规则，交叉引用编号不变 |
| 1.10.0 | 2026-08-03 | 第 17 节版本变更表迁出 `AGENTS.md`，独立为本文件；`AGENTS.md` 仅保留当前版本号与回退指引，同步更新第 15.1 节根目录元文件清单、README 结构表与根级回归集 G-003 |
| 1.9.0 | 2026-08-03 | 第 6 节压缩闭环补充：压缩摘要自动存档（会话日志与摘要/日期/压缩摘要-时分.md）并报告 7 分区与四类关键信息审计结论 |
| 1.8.0 | 2026-08-02 | 第 5 节新增详略分级：非核心内容精炼，核心数据/判断/结论推导可适当展开讲透 |
| 1.7.0 | 2026-08-02 | 第 6 节新增上下文压缩闭环：压缩后自动续接检查、长任务自动生成交接文件（自拟名）、compact_prompt 自迭代（先备份后修改，改后报告） |
| 1.6.0 | 2026-08-02 | 第 4 节新增项目外文件处理规则：一律拷贝进项目、源文件原位保留；单文件超 15MB 先询问确认再拷贝 |
| 1.5.0 | 2026-08-02 | 沟通风格调整：第 6 节第 5 条改为按需确认、不强制首行复述，新增表述混乱/信息冲突/多步操作确认触发，禁止「收到，N件事」「我先理解你的问题」等模板开场；序号与层级结构仅用于交付物正文 |
| 1.4.0 | 2026-08-02 | 一个独立小项目迁移至仓库外（git rm --cached + .gitignore），同步更新第15.1节固定目录与README结构表 |
| 1.3.0 | 2026-08-02 | 新增第16节跨会话回溯协议：平时不读其他会话/归档，显式触发才查，先项目沉淀→未归档→归档，不建MCP |
| 1.2.0 | 2026-08-02 | 应要求精简约 5% 字数：压缩长句、合并重复表述，未删强制规则 |
| 1.1.0 | 2026-08-02 | 并入提示词库复用条款：3.1 知识状态标注/五类核验清单/缺口即停/反迎合协议，5 落地方案三要素，6 隔离重判（仅高风险高难度），15 固定目录新增提示词库 |
| 1.0.1 | 2026-08-02 | 压缩至原字数约 95%：合并第 2/6/9/12/13 节与既有条款重复的表述，改为交叉引用；未删除任何强制规则 |
| 1.0.0 | 2026-08-02 | 借鉴 NGUOMD README 思想：新增版本记录、会话启动阅读顺序、项目结构与归档规则；新增根 README.md 与回归集.md；根目录业务文件按 MECE 归档至 01/02/03 目录 |
