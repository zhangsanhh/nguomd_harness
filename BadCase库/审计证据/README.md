# Bad Case 审计证据

本目录保存分批审计的机械线索、改前备份和验证证据，不替代 `BadCase库/` 下的活跃案例正文，也不作为默认任务规则加载。

## 本次审计

- 审计台账：`../闭环审计与补录台账-2026-09-09.md`
- 逐条线索：运行 `生成补录台账.py` 生成 `候选线索逐条台账-2026-09-09.tsv`
- 改前备份：`2026-09-09-路径修复前/`
- 只读校验：`../工具调用经验/闭环只读校验.py`

审计范围为 2026-08-03 至 2026-09-09。日期摘要中的关键词只用于定位，线索台账的“机械线索”不等于失败事件；只有经原要求、首次结果、纠正或验证失败和后续处理交叉核对后，才进入 BadCase 或 Pitfall。
生成器固定统计截点为 2026-09-09 15:02，避免把本次审计自身的会话日志回灌进审计样本。

改前备份仅用于回滚和差异核对；本次不删除历史摘要、旧路径证据、待归档消息或原始业务成品。

其中 `AGENTS.current-pre-closure.md` 是本次补录闭环规则改动前的精确快照；同目录的 `AGENTS.md` 是更早一次路径核查时保存的初始副本，不作为本次闭环规则回滚基线。
