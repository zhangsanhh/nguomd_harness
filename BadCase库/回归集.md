# BadCase 回归集

> 关键 Bad Case 修复验证通过后转入本集；修改 AGENTS.md、Prompt、流程或工具链后，先跑本集再全量执行。状态：待跑 / 通过 / 失败 / 不适用 / 阻断 / 证据不足。

| 编号 | 模块 | 检查点（输入 → 预期） | 状态 |
|---|---|---|---|
| R-001（预防型） | 07 会话日志 | 任意会话 → 摘要80-180字、北京时间、无竖线、索引同步 | 通过 |
| R-002（预防型） | 02 检索核验 | 时效性强提问 → 近一年内优先；通用/历史放宽；里程碑级内容不限；来源逐条标日期/类型 | 通过 |
| R-003（预防型） | 04 文档操作 | 路径/引用改动后 → 用 rg 全文检查无失效引用 | 通过 |
| R-004（预防型） | 06 流程执行 | 修改 AGENTS.md 后 → 双轮审查 + 回归集全跑 | 通过 |
| R-005（预防型） | 06 流程执行 | 任意回复 → 末尾含沉淀确认行，且“无新增”必须留有查重范围、去向判断和证据边界；有失败或复发时追加原编号 | 证据不足 |
| R-006 | 02 检索核验 | 科普/时效性文章 → 来源按时效分层，附录来源标日期与类型（BC_检索_001） | 通过 |
| R-007 | 03 内容生成 | 精选/排序类回答 → 先与既有框架做包含关系去重，再收敛到用户指定数量（BC_内容_001） | 通过 |
| R-008 | 03 内容生成 | 统计/指标类交付 → 先定义口径（起止事件、样本范围）再做数量级合理性检查（BC_内容_002） | 通过 |
| R-009 | 03 内容生成 | 面向用户的方案/规则说明 → ≤3 行、先影响后决策，不交付用户不想读的 md（BC_内容_004） | 通过 |
| R-010 | 03 内容生成 | 报告类成品 → 无元评论、章节归属正确、数据有定义与出处、代号可读、核心结论 3-5 点（BC_内容_005） | 已关闭（修复未采纳） |
| R-011 | 04 文档操作 | 网页/交互文件交付 → 先实测核心交互（点击/展开/搜索/数据加载）（BC_文档_001） | 通过 |
| R-012 | 04 文档操作 | 插图交付 → 逐图核对画面与图题/上下文一致，图下配 1-2 句分析（BC_文档_003） | 通过 |
| R-013 | 04 文档操作 | 文档任务 → 先确认目标平台与页面规则（分节页码、表格粘贴行为）（BC_文档_004） | 通过 |
| R-014 | 06 流程执行 | 减量/压缩百分比承诺 → 先扣除强制保留内容，按可动范围试算并给区间（BC_流程_001） | 通过 |
| R-015 | 06 流程执行 | 新机制/方案提案 → 先盘点既有机制与产物，说明「新增/复用/替代」（BC_流程_004） | 通过 |
| R-016 | 06 流程执行 | 配置/默认值变更 → 先给一句「改什么、为什么、影响面」再执行（BC_流程_006） | 通过 |
| R-017 | 06 流程执行 | 重建/迁移任务 → 对照原版体验基线（窗口/全屏/尺寸/交互参数）逐项实测（BC_流程_007） | 通过 |
| R-018 | 02 检索核验 | 付费墙/登录墙目标 → 先判可得性，检索同类路径 ≤2 轮即降级交付并说明合法补全路径（BC_检索_002） | 通过 |
| R-019 | 07 会话日志 | 日志时间 → 取系统时钟换算北京时间，禁止凭感觉估算（BC_日志_002） | 通过 |
| R-020 | 07 会话日志 | 任意回复结束前 → 会话日志已追加；写操作后有修改清单钩子机械证据（BC_日志_001） | 通过 |
| R-021 | 03 内容生成 | 回复开场 → 单义任务直接作答，不模板复述任务理解（§6.5／BC_内容_003） | 通过 |
| R-022 | 06 流程执行 | 独立审查模板 → 双道防线：模型自判闸门 + 用户补触发无条件启用（BC_流程_003） | 通过 |
| R-023 | 01 输入理解 | 用户提及「之前讲过/本机文档」→ 先查会话日志定位路径，再扩展本机位置（BC_输入_001） | 通过 |
| R-024 | 03 内容生成 | 多问题输入 → 回答前逐问编号、输出前逐问打勾核对；「为什么」类问题补外部理由并引导自答（BC_内容_006） | 通过 |
| R-025 | 06 流程执行 | 闲置/内存自动化 → 活跃会话不得触发 CLOSE/退出；内存动作连续 2 次 HIGH 才触发且自动处理默认 dry-run（BC_流程_008） | 通过 |
| R-026 | 05 工具调用 | 工具/命令失败后 → 先看报错快速修正 1 次；同一失败持续约 15-20 秒仍失败才 rg 查 Pitfall 库，命中照做，未命中再换路 | 通过 |
| R-027 | 05 工具调用 | Pitfall 命中解决后 → 回写「最近复用：YYYY-MM-DD」；连续 2 次命中无效改「状态：已失效，待复核」 | 通过 |
| R-028 | 05 工具调用 | 2026-08-19 执行：统计 Pitfall「最近复用」回写条数、命中率、连续无效标记数，评估双触发机制（基线：2026-08-05 有效回写 1 条） | 证据不足 |
| R-029 | 05 工具调用 | Codex 闲置关窗 → App 重启后进程运行不足阈值不得 QUIT；闲置从「会话最后写入/进程启动/最近查看」三者最新起算（Pitfall-051） | 通过 |
| R-030 | 02 检索核验 | dsh web_search → 可用（baseURL 含 /anthropic/v1）；不可用时先查 Pitfall-074 | 通过 |
| R-031 | 02 检索核验 | dsh web_fetch 不可用 → 属平台限制（Pitfall-075），用 curl 替代，不误判为配置故障 | 通过 |
| R-032 | 06 流程执行 | 涉及个人背景 → 先读 `03_生活记录/用户画像.md`；写入新画像候选前在回复底部征得同意，同意才写 | 通过 |
| R-033 | 06 流程执行 | 方案多解 → 给 A/B/C 三案＋影响面＋推荐项，人拍板后执行（GHJC-001） | 通过 |
| R-034 | 06 流程执行 | 高风险改动（删改移/多解/影响面大）→ 先副本试跑（subagent 或复制目录），人答「回行/不行」再转正（GHJC-002） | 通过 |
| R-035 | 06 流程执行 | 派发子代理 → prompt 自包含（完整上下文）、单入口、不扩展派生；并发不超上限（GJZX-004） | 通过 |
| R-036 | 06 流程执行 | 交付前 → 查文件、查状态、跑回归集；AI 自检只算第一层，以外部状态为准（ZLYS-001） | 通过 |
| R-037 | 06 流程执行 | 新机制上线 → 部署实证链四步：核格式→自测→信任→实证；自测不通过不得宣称已部署（AQDD-003） | 通过 |
| R-038 | 05 工具调用 | 动工具前 → 成本闸门四问：该不该查证（查证例外不省）/能否少调用/是否超预算/有无更省路径（CBDD-002） | 通过 |
| R-039 | 06 流程执行 | 摄像头复盘 → 先定义一个问题和变量；P优先级、E证据层、问题类型分开；不把采样、声学能量或单帧升级为健康/学习/专注结论；每次有一项动作和复测标准（BC_流程_009） | 证据不足 |
| R-040 | 03 内容生成 | 约7500字符影评／移动硬盘取材／增量修改 → 正文7125–7875字符；至少100个候选观点单元完成语义去重和来源登记；正文采用5–10个、最多12个理念；主体章节有不可互换的核心问题与独立影片锚点；增补后无重复主题章节且结语不逐章复述；「不是……而是……」不超过每450字符一次；独立审查失败最多重试一次后执行体兜底；会话日志按影片＋任务类型＋实际版本去重（BC_内容_007） | 通过 |
| R-041 | 06 流程执行 | 子代理等待／回传异常 → 每轮 `wait_agent` 120 秒后先 `read_thread` 查询 `thread.status`、`turn.status` 与最新更新时间；有进展的 `running` 继续等待；连续两个快照无进展先 `send_input(interrupt=true)` 救援并只再等待一次；仅终止或救援失败才 `close_agent`；影评任务卡只传影评、事实清单、验收标准；明确失败才重试一次；审查代理Luna/high且禁止递归派生 | 证据不足 |
| R-042 | 03 内容生成 | 影片分析 → 主体章节以具体情节、冲突或人物行动为中心，将时代/制度背景、角色社会身份、行为机制、形式证据和现实边界自然嵌入叙事；不机械拆成六个栏目，不把历史处境直接改写为个人心理或励志结论（BC_内容_008） | 通过 |
| R-043 | 05 工具调用 | 独立代理监测 → 绑定物理接口、接口不明不回落TUN；逐连接chains核验；403/421/挑战/401JSON不等于业务成功；首测与重试独立、订阅节点指纹分层、真实池轮换；到期状态/marker/报告先落盘，再归档与卸载自身，不干扰FlClash自启（Pitfall-089；test_monitor_v6.py） | 通过 |
| R-044 | 06 流程执行 | 多步/跨文件任务 → 写入目标对象、日期/版本、材料范围、交付位置、验收标准、明确排除项六项契约；关键缺口才提问，不把需求变更计为原要求失败（R-01） | 通过 |
| R-045 | 02 检索核验 | 长材料/多源任务 → 先有覆盖账本；材料类型、总量、已读、阻断、排除、重复、未读和代表样本均有记录，存在缺口时不得写“全量完成”（R-02） | 通过 |
| R-046 | 04 文档操作 | 多产物任务 → 源材料、主产物、派生产物/索引、前台交付和排除项一一映射；日记待办只前台、全天口语化建议只入手工补录（R-03） | 通过 |
| R-047 | 03 内容生成 | 增量补写/升版 → 先定位缺口，再压缩重复、替换或补充，最后做全文结构、独特信息、链接、状态和派生产物回归（R-04） | 不适用 |
| R-048 | 06 流程执行 | 阶段性产出 → 分别记录输入已读取、初稿已产出、事实/结构已验证、用户验收待定及阻断/证据不足，不把初稿当最终完成（R-05） | 通过 |
| R-049 | 06 流程执行 | 同类任务启动 → 就近读取 1—3 条相关案例并转成检查项；同一问题复发沿原条目追加，不整库加载或机械套用（R-06） | 通过 |
| R-050 | 05 工具调用 | FlClash 延迟提醒 → 分离节点原生 delay 与业务 HTTPS 总耗时；按活动配置和选择记录识别节点；隔离 Mihomo 绑定物理接口；400ms触发、连续两轮低于300ms恢复（Pitfall-093） | 通过 |
| R-051 | 05 工具调用 | FlClash告警去重 → 自动选节点在同一故障期间切换时，保留active和dismissed状态，只更新当前节点；未恢复前不得再次触发alert；两轮低于300ms后才清除抑制（Pitfall-094；monitor_flclash_latency.py 自测） | 通过 |
| R-052 | 06 流程执行 | Codex全局回合后压缩 → rollout真实`last_token_usage.input_tokens`超过300000时，Stop Hook登记请求；最终必须出现由该请求对应的`contextCompaction`，不能只检查队列或观察到其他内置压缩；低于250000解除抑制，550000运行中阈值保持不变（Pitfall-095） | 失败 |
| R-053 | 06 流程执行 | Codex回合后压缩生命周期 → Stop Hook只写pending并快速退出；独立LaunchAgent先读transcript回合边界，活动回合只延期；`active writer`只长退避，不得持续启动；最终必须记录`compact_completed`与`contextCompaction`，否则标阻断（Pitfall-096、097） | 阻断 |
| R-054 | 06 流程执行 | Codex压缩证据分层 → `compact_pending`/`compact_starting`/普通`/compact`消息不算完成；只有`context_compacted`或`item/completed`的`contextCompaction`才可标记完成 | 通过 |
| R-055 | 05 工具调用 | CUA/CDP大文本导出 → 单次大表达式失败时保留已读结果，恢复同源标签后按约2万字符分块导出，并校验字符数、字节数、哈希和结构 | 通过 |
| R-056 | 03 内容生成 | 日记 AI 总结版 → 先按时间和主题去重、核对末段覆盖，再单独检查 80%—85% 字符比例；待办和口语化建议不计入该比例（BC_内容_007） | 通过 |
| R-057 | 05 工具调用 | 项目改名后活动钩子 → 9 个命令均指向当前项目，输出脚本从自身目录推导项目根，并可在项目外临时副本完成隔离归档链（Pitfall-104） | 通过 |
| R-058（预防型） | 05 工具调用 | DSH UI 裁剪 → 关停任何 profile 行前先跑 `inject-graph-ui.mjs` 依赖图与 `preflight-ui-trim.mjs` 预检；被其它包 inject 的行不得关停；改完先 `--patch … --dump-config` 离线验证退出码 0，再在临时实例实测载荷，最后才落正式 profile（Pitfall-107） | 通过（2026-09-10 DSH 临时实例） |
| R-059（预防型） | 05 工具调用 | 改界面样式 → 只落产物样式表（`dsh-web-frontend/dist/assets/index-*.css`）＋`html body !important`；不得修改参与客户端模块图的插件 JS（如 `dsh-client-ui-theme/lib/client.js`）；改后跑 `apply-ocean-theme.mjs --check`（标记＋大括号配平）并实测页面引用 CSS 可 200 取回（Pitfall-108） | 通过（2026-09-10 DSH 临时实例） |
| R-062 | 03 内容生成 | 交互问答交付形态 → 默认只在前台回答，不生成 Markdown／不建索引／不主动问是否存档；仅用户明确要求「写成文件／存到某处」时才落文件，日记侧写为唯一例外；字数区间在前台满足即可（BC_内容_011） | 待验证 |
| R-061 | 03 内容生成 | 资料参照类回答 → 硬盘只给结论而无机制或出处、涉硬事实与数据、涉会变化的信息、或材料不足闭合论证时，须联网检索或用既有知识补足，或降级表述、或明说无依据；禁止把结论扩写成严谨机制描述，禁止以"硬盘里没有"留逻辑缺口（BC_内容_010） | 待验证 |
| R-060 | 03 内容生成 | 资料参照类回答与产物 → 来源默认一律不标注（用户凭原句回查、主动问才给）；写法像正常回答问题，直接讲观点、机制、条件与做法，不出现资料／材料／来源／检索／轨迹／RAG 等元话语，不做多方引述对账、不在结尾贴检索说明；前台、文件与派生产物同口径（BC_内容_009） | 待验证 |
| R-063 | 06 流程执行 | 影评任务生成文档 → 正文落盘 `01_内容整理/影评分析/<电影名>影评.md`，`影评分析/README.md` 三处同步，`已看电影清单.md` 补录，交付报告逐项标注状态；未落盘不得写「完成」或让 README 标「成稿」（BC_流程_010） | 待验证 |

## 本轮有限范围验证记录（2026-09-09）

| 检查范围 | 结果 | 证据边界 |
|---|---|---|
| R-044—R-046、R-048—R-049 | 通过 | 仅证明本次审计任务的契约、覆盖、映射、阶段状态和就近复用记录齐全，不证明后续所有任务均遵守 |
| R-047 | 不适用 | 本轮没有进行内容增量补写或升版 |
| R-005、R-028、R-039、R-041 | 证据不足 | 已完成文档/静态核对，但缺少长期行为分母、目标素材或真实子代理回归 |
| R-052—R-053 | 失败/阻断 | 既有 2026-09-08 记录仍显示自定义 300K 压缩未取得真实完成事件；本轮不修改该机制 |
