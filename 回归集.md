# 根级回归集

> 修改 `AGENTS.md`、README、流程或目录结构后，先跑本集再全量执行。与 `BadCase库/回归集.md` 并存：本集管根级结构与流程，子库集管失败案例修复。状态：待跑 / 通过 / 失败 / 不适用 / 阻断 / 证据不足。

| 编号 | 检查点 | 状态 |
| --- | --- | --- |
| G-001 | 根目录仅含元文件与固定目录，业务文件不堆放根目录 | 失败 |
| G-002 | 根 README 结构表与实际目录一致，新增目录已补导航 README | 证据不足 |
| G-003 | AGENTS.md 已更新当前版本号，变更记录已写入 `AGENTS版本变更.md`，且未删除既有规则 | 通过 |
| G-004 | 当前活动文件与导航/索引/AGENTS 引用同步；历史摘要、快照、备份和审计证据中的旧路径保留为证据，不作为当前活动引用 | 通过 |
| G-005 | 会话日志已按第 10 节追加并同步 HTML 索引 | 通过 |
| G-006 | 回复末尾含沉淀确认行与日志确认行 | 通过 |
| G-007 | 新交付业务文件按主旨归档（01/02/03/提示词库等），根目录不堆放（BC_文档_002） | 通过 |
| G-008 | BadCase 库各模块有真实条目，模块索引与正文状态一致，复发沿原编号追加，回归集与案例可追溯 | 通过 |
| G-009 | 沉淀确认行的“本次无新增”须基于库内实质核对、去向判断和证据边界，禁止默认输出 | 证据不足 |
| G-010 | `00_系统配置/` 规范副本与实机配置一致（`diff -r` 校验），同步清单 README 可执行 | 通过 |
| G-011 | 工具/命令失败后 → 先看报错快速修正 1 次；同一失败持续约 15-20 秒仍失败才 `rg` 检索 Pitfall 库，命中照做并回写 | 通过 |
| G-012 | 意图分级：L0 直接处理；L1 先展示一句理解再处理；L2 列假设并最多提 2–3 问后等待；L3 说明影响/回滚/验收并等待确认 | 通过 |
| G-013 | BadCase 只读校验入口 → 编号唯一、核心引用存在、状态一致、活动钩子路径有效、版本一致 | 通过 |
| G-014 | 普通任务达到长文、多源、跨文件、外部核验或复杂电脑操作等复杂度阈值 → 先披露处理骨架并等待确认；短任务直接处理；用户主动开启计划模式时不重复套用 | 证据不足（规则静态检查已完成，行为回合待验证） |
